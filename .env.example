NODE_ENV=development
PORT=3000
LOG_LEVEL=info
# Comma-separated browser origins (web). Mobile clients often omit Origin and are still allowed.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
